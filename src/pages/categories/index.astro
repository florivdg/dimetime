---
import Layout from '@/layouts/Layout.astro'
import Dashboard from '@/components/Dashboard.vue'
import CategoryManager from '@/components/categories/CategoryManager.vue'
import { getAllCategories } from '@/lib/categories'

const categories = await getAllCategories()
---

<Layout>
  <Dashboard
    client:load
    currentPath={Astro.url.pathname}
    breadcrumbs={[{ label: 'Kategorien' }]}
  >
    <div class="flex flex-1 flex-col gap-4">
      <div class="flex items-center justify-between">
        <h1 class="text-2xl font-semibold tracking-tight">Kategorien</h1>
      </div>
      <CategoryManager client:load initialCategories={categories} />
    </div>
  </Dashboard>
</Layout>
